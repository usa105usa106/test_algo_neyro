# Gmail 66 test report

Проверено:

1. nginx-конфигурация синтаксически валидна.
2. Порт `80` отвечает на независимый `/router-healthz`.
3. `/healthz` и `/gmail/callback` проксируются на внутренний порт `8080`.
4. Compose route остаётся `SERVICE_URL_GMAIL-AUTH_80`.
5. Негmail-файлы режимов и task не изменялись относительно версии 65.

Окончательная проверка внешнего домена и Traefik выполняется после полного Redeploy на VPS.
